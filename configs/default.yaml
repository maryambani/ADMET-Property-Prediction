seed: 42

data:
  dataset: tox21
  fingerprint_radius: 2
  fingerprint_bits: 2048

model:
  hidden_dims: [512, 256]
  dropout: 0.5

training:
  batch_size: 64
  lr: 0.001
  epochs: 50
  patience: 5
  val_split: 0.15
  test_split: 0.15

wandb:
  enabled: false
  project: admet-tox21
